<%= form_for :event, url: event_edit_path(@event), method: :put do |f|%>
	<p>
		<%= f.label :description %>
		<%= f.text_field :description %>
	</p>
	<p>
		<%= f.label :invitee %>
		<%= f.text_field :invitee %>
	</p>

	<p>
		<%= f.submit%>
	</p>
<%end%>